version: 2
jobs:
  diana:
    docker:
      - image: circleci/python:3.6.1

    steps:
      - checkout

      - run: echo "Install dependence"
      - run: cp .env.sample .env
      - run: python3 -m venv venv
      - run: . venv/bin/activate
      - run: pip install -r requirements.txt

      - run: echo "Django test"
      - run: . venv/bin/activate
      - run: python manage.py test

  kotex:
    docker:
      - image: circleci/python:3.6.1

    steps:
      - checkout
      - run: echo "Hi Long, Hehe"

workflows:
  version: 2
  diana_kotex:
    jobs:
      - diana
      - kotex